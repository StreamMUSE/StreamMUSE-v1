# Source

Copied from `/data/home/yuanxin/lekai/real_time/prompt_model` into StreamMUSE for the `lekai_prompt_continuation` integration branch.

Only import paths were adapted to package-relative imports so the code can be imported as part of `streammuse`.
